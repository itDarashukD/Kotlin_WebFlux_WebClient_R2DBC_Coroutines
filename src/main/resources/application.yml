server:
  port: 8081

spring:
  cache:
    caffeine:
      spec: expireAfterAccess=600s
    cache-names: breeds
  sql:
    init:
      mode: always
#  datasource:
#    url : jdbc:h2:mem:testdb
#    driverClassName : org.h2.Driver
#    username : sa
#    password : sa
#    url: jdbc:h2:./src/test/resources/~;MODE=PostgreSQL

# Use properties below for setting up r2dbc:
  r2dbc:
    url: r2dbc:h2:file:///./src/test/resources/db/~;MODE=PostgreSQL
#    url: r2dbc:h2:mem:///testdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
#    username: sa
#    password: sa

management:
  endpoint:
    prometheus:
      enabled: true
    metrics:
      enabled: true
  endpoints:
    web:
      exposure:
        include: [ "prometheus", "metrics"]


#logging:
#   level:
#     org:
#       springframework : debug